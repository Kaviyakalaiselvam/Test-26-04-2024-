let numbers=[1,2,3];
let multiplication=[];
for(let i = 0; i < numbers.length; i++) {
    if (numbers[i] * 2 )
    multiplication.push(numbers[i]*2);
}
console.log(`Multiple of number is: ${multiplication}`);
